import { add } from '../src/index';

describe('add index suite', () => {
  it.todo('add some more tests');
  it.todo('add some more tests');

  it('should add 2 numbers and return the sum', () => {
    const sum = add(1, 2);
    expect(sum).toBe(3);
  });
});
